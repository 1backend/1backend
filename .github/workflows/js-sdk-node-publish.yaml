name: "@1backend/sdk-node publish to npm"

on:
  push:
    branches:
      - main

jobs:
  publish:
    uses: ./.github/workflows/js-publish-reusable.yaml
    with:
      package_dir: ./sdk/node
    secrets: inherit
